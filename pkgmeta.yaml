package-as: BetterDeathRecap
manual-changelog: CHANGELOG.md

# ⚠ Keep this list in sync with the EXCLUDES array in package.sh
ignore:
  - .git/
  - .github/
  - .idea/
  - .claude/
  - docs/
  - dist/
  - CLAUDE.md
  - PLAN.md
  - .editorconfig
  - .gitattributes
  - .gitignore
  - .luacheckrc
  - Makefile
  - package.sh
  - "*.zip"
  - "*.tar.gz"
  - "*.svg"   # design mocks only — WoW can't load SVG (e.g. media/betterdeathrecap_icon.svg)
